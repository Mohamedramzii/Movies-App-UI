/*Positioned(
                top: 0,
                left: 0,
                child: Container(
                  height: height * 0.55,
                  width: width,
                  decoration: BoxDecoration(
                      image: DecorationImage(
                          image: AssetImage(movie!.moviePoster),
                          fit: BoxFit.fill)),
                )),*/



/**/


    /*  */

    /* Positioned(
                bottom: splitted_moviename.length <= 2
                    ? height * 0.39
                    : height * 0.36,
                left:
                    splitted_moviename.length <= 2 ? width * 0.36 : width * 0.2,
                child: ConstrainedBox(
                  constraints: BoxConstraints(
                    maxWidth: splitted_moviename.length >= 3
                        ? width * 0.6
                        : width * 0.4,
                    minWidth: width * 0.1,
                  ),
                  child: Text(
                    movie!.movieName,
                    textAlign: TextAlign.center,
                    // splitted_moviename.length.toString(),
                    style: TextStyle(fontSize: 25.sp, color: Colors.white),
                  ),
                )), */


    /*  Positioned(
                bottom: height * 0.31,
                left: width * 0.12,
                child: Text(
                  '${movie!.movieReleaseYear}⚪ ${movie!.movieCategory}⚪ ${movie!.movieDuration}',
                  style: TextStyle(color: Colors.white),
                ))*/